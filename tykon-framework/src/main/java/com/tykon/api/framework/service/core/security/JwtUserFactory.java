package com.tykon.api.framework.service.core.security;

public final class JwtUserFactory {

	private JwtUserFactory() {
	}



	/*
	 * private static List<GrantedAuthority>
	 * mapToGrantedAuthorities(List<RoleActionLink> roleActionLinks) { return
	 * roleActionLinks.stream() .map(authority -> new
	 * SimpleGrantedAuthority(String.valueOf(authority.getActionsId())))
	 * .collect(Collectors.toList()); }
	 */
}
